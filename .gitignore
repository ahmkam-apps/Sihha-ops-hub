data/
__pycache__/
*.pyc
*.pyo
.env
.DS_Store
*.db
*.db-wal
*.db-shm
venv/
.venv/
.pytest_cache/
pytest-cache-files*/

# nixpacks.toml stays ignored — committing it previously caused "No module named pip"
# build failures on Railway. Python version is pinned via .python-version instead
# (nixpacks reads it natively).
nixpacks.toml
